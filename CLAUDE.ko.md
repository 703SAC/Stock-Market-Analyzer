# CLAUDE.ko.md

이 저장소에서 코딩 에이전트가 따라야 할 행동 원칙과 프로젝트 제약입니다.

주의: 이 지침은 속도보다 안정성을 우선합니다. 사소한 작업에서는 과도한 절차를 생략할 수 있습니다.

## 프로젝트 컨텍스트

- 핵심 앱 코드는 `app/backend`(FastAPI), `app/frontend`(Next.js)에 있습니다.
- `open-trading-api`는 외부 샘플 소스입니다. 명시적 요청이 없으면 수정하지 않습니다.
- `kis-ai-extensions`는 개발 보조/참고용입니다. 앱 런타임 의존성으로 추가하지 않습니다.
- 계층 경계를 유지합니다:
  - Router -> Feature Service -> Adapter -> External API
- 비밀값(KIS, NAVER, OPENAI, GOOGLE)은 로그/응답/커밋에 노출하지 않습니다.

## 1. Think Before Coding

- 가정을 숨기지 말고 먼저 명시합니다.
- 해석이 여러 개면 옵션을 제시하고 확인합니다.
- 더 단순하거나 안전한 대안이 있으면 구현 전에 제안합니다.
- 요구사항이 프로젝트 제약과 충돌하면 중단하고 확인합니다.

## 2. Simplicity First

- 오늘의 요구를 만족하는 최소 구현만 작성합니다.
- 추측성 확장(미래 대비 추상화, 과한 설정)은 넣지 않습니다.
- 기존 코드 패턴과 동일한 방식으로 확장합니다.

## 3. Surgical Changes

- 요청과 직접 관련된 파일만 수정합니다.
- 주변 리팩터링/포맷팅/스타일 교체를 하지 않습니다.
- 내 변경으로 생긴 미사용 코드만 정리합니다.

## 4. Goal-Driven Execution

- 성공 기준을 검증 가능한 형태로 먼저 정의합니다.
- 버그는 재현 -> 수정 -> 회귀 확인 순서로 처리합니다.
- 기능은 입력/출력 계약을 먼저 고정하고 테스트합니다.

권장 루프:
1. 기대 동작 정의(재현 또는 테스트).
2. 최소 변경 구현.
3. 변경 범위 검증.
4. 인접 회귀 확인.